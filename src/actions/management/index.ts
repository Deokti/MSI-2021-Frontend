import { createAction } from "redux-actions";

// Management
export const setActiveControl = createAction('SET_ACTIVE_CONTROL');
export const setSupActiveControl = createAction('SET_SUP_ACTIVE_CONTROL');